@model IEnumerable<TSGTS.Core.DTOs.InvoiceDto>
@{
    ViewData["Title"] = "Faturalar";
}

<div class="card mb-3">
    <div class="card-body d-flex justify-content-between align-items-center">
        <div>
            <h4 class="mb-1">Faturalar</h4>
            <div class="text-muted">Servis kayıtlarına ait faturalar</div>
        </div>
        <a class="btn btn-primary" asp-action="Create">Yeni Fatura</a>
    </div>
</div>

<div class="card">
    <div class="card-body p-0">
        <table class="table mb-0">
            <thead>
                <tr>
                    <th>Fatura #</th>
                    <th>Kayıt Id</th>
                    <th>Net Tutar</th>
                    <th>Tarih</th>
                    <th class="text-end">İşlem</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Id</td>
                    <td>@item.TicketId</td>
                    <td>@item.FinalAmount.ToString("C")</td>
                    <td>@item.InvoiceDate.ToString("dd.MM.yyyy")</td>
                    <td class="text-end">
                        <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-outline-danger" onclick="return confirm('Silinsin mi?');">Sil</button>
                        </form>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
</div>
